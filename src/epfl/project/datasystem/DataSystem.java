package epfl.project.datasystem;

import java.io.File;

/**
 * Simple abstraction for storing and retrieving data.
 * Self management of the memory (fast or slow).
 * @author Loic
 */
public class DataSystem {
    private byte[][] bufferMatrix;
    
    public static void storeData(int fid, byte[] data) {
        
    }
    
    public static void storeData(int fid, File file, byte[] data) {
        
    }
    
}
